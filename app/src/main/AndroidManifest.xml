<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.news">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <meta-data
        android:name="com.google.android.gms.version"
        android:value="@integer/google_play_services_version" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/nlogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.News">
        <activity android:name=".Activity.AllCategories"></activity>
        <activity android:name=".Activity.OnboardingFragment" />
        <activity android:name=".Activity.Dashboard" />
        <activity android:name=".Remote.NewsDetailsActivity" />
        <activity android:name=".Adapter.ListNewsActivity" />
        <activity android:name=".Activity.VerifyGenerateOTP" />
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.SignInActivity"/>
        <activity android:name=".Activity.ChannelNewsActivity" />
    </application>

</manifest>